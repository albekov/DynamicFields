@model DynamicFields.Controllers.EditFieldViewModel

@{
    ViewBag.Title = "Edit field";
}

<h2>Add field</h2>

@using (Html.BeginForm("Add", "Fields", new {@class = "form"}))
{
    @Html.Partial("_Edit", Model)
    <input type="submit" class="btn btn-default" value="Create" />
}