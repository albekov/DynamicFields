@model DynamicFields.Controllers.FormsListViewModel

@{
    ViewBag.Title = "Forms";
}

<h2>Dynamic forms @Html.ActionLink("Add", "Add", null, new {@class = "btn btn-info"})</h2>

<table class="table table-hover">
    <thead>
    <tr>
        <th>Name</th>
        <th>Description</th>
        <th></th>
    </tr>
    </thead>
    <tbody>
    @foreach (var f in Model.Forms)
    {
        <tr>
            <td>@f.Name</td>
            <td>@f.Description</td>
            <td>@Html.ActionLink("edit", "Edit", new {id = f.Id})</td>
        </tr>
    }
    </tbody>
</table>